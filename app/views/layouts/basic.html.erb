<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title><%= content_for?(:title) ? yield(:title) : page_title %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">
    <meta http-equiv="X-UA-Compatible" content="IE=10; IE=9; IE=8; IE=7; IE=EDGE" />
    <!-- HTML5 shim, for IE6-8 support of HTML5 elements -->
    <!--[if lt IE 9]>
      <script src="https://html5shiv.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= csrf_meta_tags %>

    <%= head if respond_to? :head %>
    <%= yield :head %>
  </head>
  <body class="<%= body if respond_to? :body %> main-content" data-no-turbolink="<%= turbolink if respond_to? :turbolink %>">
    <%= yield %>

    <%= javascript_include_tag "application" %>
    <%= javascript if respond_to? :javascript %>
    <%= yield :js %>
    <%= yield :js2 %>
  </body>
</html>
